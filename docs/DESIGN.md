# Design decisions

**Agent = explicit state machine, not LangGraph.** LangGraph isn't installed and the loop is small; a ~300-line
state machine (`backend/agent/graph.py`) with a transition table (`state.py`) is easier to explain and to test
(`REPAIRING → RESOLVED` is unrepresentable).

**The LLM chooses tools; code owns truth.** The model picks the next read-only tool and proposes a diagnosis. Tool results
carry findings generated by code from real data; the model can only cite finding IDs. `diagnosis.validate` decides whether a
diagnosis is admissible. There is no if/else diagnosis tree — the manifest comparison only *labels* observations as
anomalies, it never names a root cause or picks a repair.

**Why a "manifest".** "Missing subscriber" is only an anomaly relative to what the robot should look like. `demo_robot/manifest.json`
is the healthy reference (nodes, topics, rates, TF, parameters). It contains no fault information.

**Confidence.** The LLM's own confidence is never shown. The UI shows an *evidence score* computed in code from the number
and diversity of cited anomalies, labelled heuristic, with the breakdown visible.

**No hidden fault identity.** The supervisor stores ground truth in `demo_robot/state/ground_truth.json` for scoring only.
The backend never reads it; the API returns `hidden` for random faults; the agent's inputs are ROS observations only.
`get_component_status` exposes process state and exit codes (what an operator's process manager shows) but not launch
arguments, which would reveal `topic_misconfig`.

**Approval binding.** A proposal ID is bound to `(action, target)` and consumed on use; the repair executor re-checks
the allowlist *and* the approval, so a bug in the agent cannot bypass either.

**Verification is broader than the repaired component.** After a restart the verifier re-measures the whole robot (24 checks)
so a wrong fix — or a fix that breaks something else — does not count as recovery. A failed verification triggers one more
investigation round, then a safe stop.

**Private reasoning is not exposed.** Ollama's `thinking` output is discarded; `<think>` blocks are stripped from content and
never appended to history or shown. The timeline shows tool calls, evidence and short summaries.

**DDS settings for WSL2** (`config/cyclonedds.xml`): fragment below the MTU (WSL mirrored networking drops IP fragments, which
silently ate every `LaserScan`) and a 3 s participant lease so a crashed node leaves the graph in ~4 s instead of ~11 s.
Domain 73 + localhost discovery keeps RobotOps isolated from other ROS systems on the machine.

**Thinking off by default** (`ROBOTOPS_THINK=1` to enable): qwen3:4b steps take ~10 s instead of ~25–30 s; diagnoses were the
same in our comparison.
